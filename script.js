document.querySelectorAll('.requirement-form').forEach(form=>{const params=new URLSearchParams(location.search);const product=form.querySelector('[name="product"]');if(product&&params.get('product'))product.value=params.get('product');form.querySelector('button')?.addEventListener('click',()=>alert('This demo form is ready to be connected to your preferred form service. You can also email sales@kaysonstechno.com.'));});