/*
  Public website integration settings.
  Complete the values below after creating the EmailJS and Google Analytics accounts.
  EmailJS public keys are designed for browser use; never place private email passwords here.
*/
window.KAYSONS_SITE_CONFIG = Object.freeze({
  emailjs: Object.freeze({
    publicKey: "",
    serviceId: "",
    templateId: "",
  }),
  analytics: Object.freeze({
    measurementId: "",
  }),
});
